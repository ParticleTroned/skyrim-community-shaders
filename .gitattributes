# Normalize all text files to LF
* text=auto eol=lf

# Ensure patch files always use LF line endings
*.patch text eol=lf
# Complete ledger cells are audited by the reporting tool; text diffs are oversized.
docs/development/vr-render-scale-*ledger*.csv -diff
